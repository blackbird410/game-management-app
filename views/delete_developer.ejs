<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete Developer Confirmation</title>
  <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-6 text-gray-900">
    <div class="max-w-lg mx-auto bg-white p-8 rounded-lg shadow-md">
      <h1 class="text-2xl font-bold text-red-600 mb-4 text-center">Confirm Developer Deletion</h1>
      <p class="text-gray-700 mb-6 text-center">
        Are you sure you want to delete the developer "<strong><%= developer.name %></strong>"? This action cannot be undone.
      </p>

      <form action="/admin/devs/<%= developer.id %>/delete" method="POST" class="space-y-4">
        <div class="flex justify-between">
          <button type="submit" class="px-4 py-2 bg-red-600 text-white font-semibold rounded-md shadow-sm hover:bg-red-700 w-full sm:w-auto">
            Confirm Delete
          </button>
          <a href="/admin/devs" class="px-4 py-2 bg-gray-300 text-gray-700 font-semibold rounded-md shadow-sm hover:bg-gray-400 w-full sm:w-auto text-center">
            Cancel
          </a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>

